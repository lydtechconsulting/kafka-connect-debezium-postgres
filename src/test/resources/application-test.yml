spring:
    datasource:
        driverClassName: org.h2.Driver
        url: jdbc:h2:mem:testdb;SCHEMA_SEARCH_PATH=kafka_connect_demo_debezium_postgres;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: none
            show_sql: true
